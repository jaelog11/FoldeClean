# FoldeClean Standard / Pro 기능 구분 (안)

작성 2026-09-13. 유료화 전 기능 경계를 정하기 위한 분석.

## 1. 나누는 원칙

정리 유틸리티 시장(File Juggler, DropIt, Hazel, Belvedere, PhotoMove 등)에서 검증된 기준을 그대로 따른다.

1. **안전은 절대 유료가 아니다.** 연결 파일 감지, 미리보기, 되돌리기, 기록은 무료판에 전부 들어간다. 안전을 팔면 "무료판은 위험한 프로그램"이라는 인상을 주고, 사고가 나면 신뢰가 무너진다.
2. **무료판만으로도 한 번의 정리는 완벽해야 한다.** 무료판이 좋아야 다운로드 수가 늘고, 그 사용자 중 일부가 Pro를 산다. 반쪽짜리 무료판은 홍보 효과가 없다.
3. **Pro는 "시간을 아껴 주는 것"과 "힘"을 판다.** 자동화(안 해도 되게), 규칙(내 방식대로), 규모(더 많이·더 빠르게), 통찰(무엇을 정리할지 알려줌). 이 넷 중 하나에 들어가야 Pro 기능이다.
4. **Pro 기능은 무료판에서 보이되 잠겨 있어야 한다.** "이런 게 있구나"를 알아야 산다. 카드에 Pro 표식을 달고, 누르면 무엇을 해 주는지 설명과 구매 안내를 보여준다.
5. **1회 결제, 구독 없음.** 정리 프로그램은 매일 쓰는 도구가 아니라 구독 저항이 크다. 대신 큰 버전(1.x → 2.x) 업그레이드 때 할인 판매로 재수익을 낸다.

## 2. 현재 기능 분류

| 기능 | Standard(무료) | Pro | 근거 |
|---|:-:|:-:|---|
| 폴더 검사, 파일 구성 통계 | ● | ● | 기본 |
| 연결 파일 감지 (시스템·레지스트리·바로가기·잠김·프로젝트·클라우드) | ● | ● | 원칙 1 |
| 연결 파일 묶음 표시 | ● | ● | 원칙 1 |
| 정리 방식 7가지 (종류·날짜·종류→연도·PARA·Johnny·오래된 파일·중복) | ● | ● | 원칙 2. 방식 자체는 경쟁 무료 도구도 다 있음 |
| 이동 경로 흐름도, 폴더 구조 미리보기, 이동 목록·개별 제외 | ● | ● | 원칙 1 |
| 파일 열기·탐색기 보기·중복 원본 보기 | ● | ● | 원칙 1 |
| 실행, 진행바, 매 건 기록, 되돌리기 | ● | ● | 원칙 1 |
| 4개 언어 | ● | ● | 홍보 자산 |
| 오류 보고서 저장 | ● | ● | 지원 비용 절감 |
| 한 번에 한 폴더, 정리 결과는 같은 폴더 안 | ● | | 무료판의 자연스러운 한계 |

결론: **지금 있는 기능은 전부 Standard**다. 안전과 기본 정리이기 때문이다. Pro로 팔 만한 기능은 아직 없다. 따라서 Pro는 새 기능으로 만들어야 하며, 로드맵 0.4~0.6의 항목 대부분이 그 후보다.

## 3. Pro 기능 제안

우선순위는 "사용자가 돈을 낼 이유의 크기 ÷ 만드는 비용"으로 매겼다. 규모: S(며칠) M(1~2주) L(3주 이상).

### 3-1. 1차 출시 Pro (이 4가지면 팔 수 있다)

> 진행 상황 (2026-09-16): **P1 규칙 편집기 완료** (0.2). 판 구분(Standard/체험 14일/Pro) 골격과 잠금 표시도 함께 들어갔다. 다음은 P2 프로필.


| # | 기능 | 무엇을 해 주나 | 원칙 | 규모 |
|---|---|---|---|---|
| P1 | **규칙 편집기** | "파일명에 '영수증' → 문서/영수증", "확장자 psd·ai → 디자인/원본", "100MB 이상 동영상 → 외장하드" 같은 규칙을 화면에서 만들어 정리 방식과 함께 적용. 조건은 이름 패턴·확장자·크기·날짜·출처 폴더, 동작은 폴더 지정·이름 앞뒤 붙이기·건너뛰기. | 규칙 | M |
| P2 | **정리 프로필과 원클릭 실행** | 폴더 + 방식 + 옵션 + 규칙을 "다운로드 주간 정리"처럼 저장. 첫 화면에서 한 번 눌러 미리보기까지 바로 간다. 여러 폴더를 한 프로필에 묶어 한 번에 정리. | 자동화·규모 | S |
| P3 | **예약 실행과 폴더 감시** | 트레이에 상주하며 정한 시각(매주 월요일 9시) 또는 폴더에 파일이 들어올 때 프로필을 돌린다. 기본은 "미리보기만 만들고 알림", 신뢰가 쌓이면 "자동 실행" 선택. Windows 작업 스케줄러 등록. | 자동화 | M |
| P3b | **중복 다운로드 감지** (P3의 일부) | 감시 중인 폴더(예: 다운로드)에 새 파일이 완성되면 크기가 같은 기존 파일과 내용을 비교한다. 완전히 같으면 즉시 알림: "방금 받은 파일은 이미 있는 ○○와 같습니다." 처리 방식은 설정: 물어보기(기본) / 새 사본을 휴지통으로 / `_중복` 폴더로. 이름이 달라도 내용이 같으면 잡는다. 저장 자체를 막지는 않는다(다른 프로그램의 저장 행위에는 끼어들 수 없고, 막으면 백신과 충돌). | 자동화 | S (P3 위에 며칠) |
| P4 | **다른 위치로 정리 + 여러 폴더** | 정리 결과를 다른 드라이브·외장 하드·NAS로 보내기(같은 드라이브면 이동, 다르면 복사 후 검증 후 삭제). 바탕화면 + 다운로드 + 문서를 한 번에. | 규모 | S |

이 4개는 서로 이어진다. 규칙(P1)을 프로필(P2)에 담고, 프로필을 예약(P3)으로 돌리며, 결과를 원하는 곳(P4)에 둔다. "내 방식대로, 자동으로, 어디로든"이 Pro의 한 줄 설명이 된다.

### 3-2. 2차 Pro (판매 후 업데이트로 추가, 재구매 유도)

| # | 기능 | 무엇을 해 주나 | 원칙 | 규모 |
|---|---|---|---|---|
| P5 | **사진·음악 메타데이터 정리** | 사진은 촬영일(EXIF)·카메라·장소로, 음악은 가수/앨범 태그로 폴더를 만든다. 수정일이 아니라 진짜 정보로 정리. | 규칙 | M |
| P6 | **비슷한 사진 찾기** | 내용이 완전히 같지 않아도 거의 같은 사진(연사, 리사이즈본, 편집본)을 지각 해시로 묶어 보여주고 최적본만 남기기 제안. | 통찰 | M |
| P7 | **디스크 지도 (트리맵)** | 어떤 폴더·파일이 용량을 차지하는지 한 화면에 그림으로. 큰 파일·오래된 큰 파일·중복을 색으로 표시하고 거기서 바로 정리. | 통찰 | M |
| P8 | **찌꺼기 정리** | 빈 폴더, 임시 파일(~$, .tmp, Thumbs.db, .DS_Store), 0바이트 파일, 깨진 바로가기를 찾아 휴지통으로. | 자동화 | S |
| P9 | **스마트 보관(압축)** | 오래된 파일을 Archive 폴더로 옮기는 데서 나아가 연도별 zip으로 묶어 용량을 줄인다. 목록은 그대로 검색 가능. | 규모 | M |
| P10 | **바로가기 자동 수정** | 바로가기가 가리키는 파일을 옮길 때 바로가기(.lnk)도 새 위치로 고친다. "주의" 파일을 안심하고 포함할 수 있게 된다. | 힘 | S |
| P11 | **일괄 이름 바꾸기** | 정리하면서 "2025-03-12_회의록.docx"처럼 날짜 접두어·연번·소문자 통일 등 규칙으로 이름 정리. | 규칙 | S |
| P12 | **정리 보고서와 통계** | 정리 전후 용량, 확보 공간, 월별 정리 이력을 화면과 HTML/엑셀 보고서로. 회사에서 "정리했다"를 증빙하는 용도. | 통찰 | S |
| P13 | **명령줄(CLI)** | `foldeclean run "다운로드 정리"` 로 스크립트·서버·다른 도구에서 호출. 파워유저와 회사용. | 자동화 | S |

### 3-3. 검토했지만 뺀 것

- 광고, 사용 횟수 제한, 파일 수 제한: 무료판을 나쁘게 만들어 원칙 2에 어긋난다.
- 클라우드 동기화·계정: 서버 비용과 개인정보 부담이 생긴다. "아무것도 외부로 보내지 않는다"는 장점을 잃는다.
- AI 자동 분류: 매력적이지만 API 비용이 들어 1회 결제와 맞지 않고, 파일 내용을 외부로 보내야 한다. 나중에 로컬 모델이 가벼워지면 재검토.

## 4. 가격과 라이선스 (안)

- Pro 1회 결제 **9,900원** (해외 7.99달러). 정리 유틸리티의 체감 상한선은 1만 원대 초반.
- 무료판 안에서 Pro **14일 체험**. 체험이 끝나면 Pro 기능만 잠기고 정리한 결과는 그대로.
- 결제·라이선스는 Microsoft Store 인앱 구매(추가 기능)로 시작. 키 발급·환불·세금을 Store가 처리. 해외 판매가 늘면 Gumroad 라이선스 키를 추가.
- 프로그램 안 구조: `Edition` 상태 하나(Standard/Trial/Pro)와 `IsPro(feature)` 확인 함수. Pro 카드는 항상 보이고, 잠긴 상태에서 누르면 설명 + 체험 시작 + 구매 버튼.

## 5. 로드맵 반영

- 0.2 배포 준비(winget) → 무료판 사용자 확보. 변경 없음.
- 0.3 신뢰 → 무료판 유지. (바로가기 자동 수정 P10은 Pro로 이동)
- 0.4 = **Pro 1차**: P1 규칙 편집기, P2 프로필, P4 다른 위치·여러 폴더, Edition 골격과 체험. 여기서 Store 등록.
- 0.5 = **Pro 자동화**: P3 예약·감시(트레이), P8 찌꺼기 정리, P12 보고서.
- 0.6 = **Pro 미디어·통찰**: P5 메타데이터, P6 비슷한 사진, P7 디스크 지도.
- 1.0 배포 완성: 서명, 자동 업데이트, P13 CLI.

## 6. 요약

- 지금 기능은 전부 무료판이 맞다. 안전과 기본 정리를 팔면 안 되기 때문이다.
- Pro는 "내 방식대로(규칙), 자동으로(프로필·예약), 어디로든(다른 위치·여러 폴더)" 세 가지로 시작한다. 만드는 데 대략 4~6주.
- 이후 사진·음악 메타데이터, 비슷한 사진, 디스크 지도가 2차 Pro의 핵심이고, 이것이 Pro를 "정리 도구"에서 "용량 관리 도구"로 넓힌다.
