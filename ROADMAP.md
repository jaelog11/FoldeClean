# 업그레이드 계획 (제안)

> Standard(무료)/Pro 기능 구분과 Pro 후보 기능은 `EDITIONS.md` 참고. 0.4부터 Pro 기능을 만든다.

지금 0.1은 "안전하게 옮기고 되돌린다"는 핵심이 완성된 상태입니다. 다음 단계는 **사용자가 덜 고민하게** 만드는 방향으로 잡는 것이 좋습니다. 아래는 효과가 크고 만들기 쉬운 순서입니다.

## 0.2 · 배포 준비 (winget) ← 진행 중
배포 채널은 **winget**(Windows 패키지 관리자)으로 정했다. `winget install Jaelog.FoldeClean` 한 줄로 설치되고 업데이트도 자동이다.
- [x] 이동 기록 매 건 저장 (.jsonl) + 강제 종료 후 복구
- [x] 첫 실행 안내 화면 (4개 언어)
- [x] 오류 보고서 저장 버튼 + 오류 로그
- [x] 라이선스(LICENSE.txt), winget 매니페스트 3종, 배포 묶음 스크립트(release.ps1)
- [ ] 백신 예외 등록 후 빌드 → VirusTotal 확인 → 오탐 신고 (Bitdefender, Microsoft 등)
- [ ] 다른 환경 시험: OneDrive/구글 드라이브 폴더, 네트워크 드라이브, 외장 하드, 권한 없는 폴더, 긴 경로, 이모지 파일명
- [ ] GitHub 저장소 + Release(zip 첨부) → `winget validate` → winget-pkgs 에 Pull Request
- [ ] 영어 설명서(README.en.md)와 문의 창구(GitHub Issues)

## 0.3 · 안심하고 쓰기 (신뢰)
- **바로가기 자동 수정**: 바로가기가 가리키는 파일을 옮길 때 바로가기(.lnk)도 새 위치로 고쳐 준다. 지금은 "주의"로만 표시.
- **드라이런 보고서 저장**: 미리보기 내용을 HTML/엑셀로 저장해 실행 전에 다른 사람과 검토.
- **실행 중 오류 상세**: 실패한 파일을 목록으로 보여주고, 사용 중인 파일은 "나중에 다시 시도" 버튼 제공.
- **정리 제외 규칙**: 특정 폴더·확장자·이름 패턴을 항상 건너뛰도록 저장 (예: `작업중/`, `*.psd`).

## 0.4 · 더 똑똑한 분류
- **사용자 규칙 편집기**: "파일명에 '영수증'이 있으면 문서/영수증으로" 같은 규칙을 화면에서 추가. 정리 방식과 함께 적용.
- **사진 촬영일 기준 날짜 정리**: 수정일 대신 사진 안의 촬영 정보(EXIF)를 읽어 정확한 연/월로.
- **유사 파일 찾기**: 내용이 완전히 같지 않아도 이름·크기가 비슷한 파일(v1, v2, 복사본)을 묶어 보여주기.
- **빈 폴더·임시 파일 정리**: `~$`, `.tmp`, `Thumbs.db` 등 안전하게 지울 수 있는 찌꺼기 제안.

## 0.5 · 습관으로 만들기
- **정리 프로필 저장**: 폴더 + 방식 + 옵션을 이름 붙여 저장하고 한 번에 실행 (예: "다운로드 주간 정리").
- **예약 실행**: Windows 작업 스케줄러에 등록해 매주 자동으로 미리보기만 만들고 알림. 실행은 사람이 확인.
- **폴더 감시 모드**: 다운로드 폴더에 파일이 들어오면 즉시 분류 제안 (실행은 확인 후).
- **중복 다운로드 감지**: 감시 폴더에 내용이 같은 파일이 또 내려오면 즉시 알림. 물어보기 / 휴지통 / `_중복` 폴더 중 선택. (EDITIONS.md P3b)
- **통계 화면**: 정리 전후 용량, 확보한 공간, 월별 정리 기록.

## 0.6 · 규모와 환경
- **대용량 최적화**: 10만 개 이상 폴더에서 목록·흐름도 지연 없는지 측정하고 개선. 해시는 xxHash로 교체.
- **OneDrive·네트워크 드라이브 대응**: 동기화 중 이동 충돌 방지, 자리표시자 파일은 정리 대상에서 제외하거나 경고.
- **여러 폴더 동시 정리**: 바탕화면 + 다운로드를 한 번에.
- **다국어**: 영어 화면 추가 (문구를 한 파일로 분리).

## 1.0 · 배포
- 설치 프로그램(MSIX 또는 Inno Setup)과 코드 서명으로 "알 수 없는 게시자" 경고 제거.
- 자동 업데이트 확인.
- 도움말 페이지와 첫 실행 안내.

## 만들 때 지킬 원칙
1. **기본값은 항상 안전하게**: 새 기능이 파일을 지우거나 덮어쓰면 안 된다. 삭제 대신 이동, 실행 전 미리보기, 되돌리기 필수.
2. **테스트는 가짜 자료로**: `make_sample.py` 에 새 상황(바로가기, EXIF 사진, 임시 파일)을 추가하고 `--selftest` 로 검증.
3. **버전 규칙**: 기능이 늘면 0.x 를 올리고, 버그만 고치면 번호를 유지한 채 CHANGELOG 에만 적는다.
