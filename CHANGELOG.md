# 변경 이력

버전은 0.1부터 시작해 기능이 더해질 때마다 0.2, 0.3 … 으로 올립니다.
버전 번호는 `FoldeClean/FoldeClean.csproj` 의 `<Version>` 한 곳에서만 바꾸면 exe 속성·창 제목·화면에 모두 반영됩니다. 파이썬 판은 `app.py` 의 `VERSION` 을 같이 맞춥니다.

## 0.2 (작업 중) 배포 준비
- 이동 기록을 한 건마다 즉시 저장(.jsonl). 강제 종료돼도 "되돌리기 기록"에서 복구·되돌리기 가능
- 첫 실행 안내 화면 (실행 전 무변경, 자동 제외, 기록·되돌리기, 작은 폴더로 시험)
- 왼쪽 아래 "오류 보고서 저장" 버튼: 바탕화면에 보고서 저장, 오류 로그(%LOCALAPPDATA%\FoldeClean\logs) 포함
- 중복 검사 해시를 SHA256 → xxHash128 (더 빠름, 백신 휴리스틱 회피)
- 배포: LICENSE.txt, winget 매니페스트(winget/), release.ps1, 폴더 형태 빌드(build.cmd folder), 서명 스크립트(sign.cmd, make_cert.ps1)

## 0.1 (2026-09-12) 첫 버전
- 5단계 화면: 폴더 선택 → 검사 → 정리 방식 → 미리보기 → 실행
- 연결 파일 감지: 시스템 폴더, 프로그램 폴더, 레지스트리 등록 경로, 사용 중인 파일(차단) / 바로가기 대상, 실행파일, 프로젝트 폴더, 클라우드 동기화, 숨김(주의)
- 정리 방식 7가지: 종류별, 날짜별, 종류→연도, PARA, Johnny.Decimal, 오래된 파일 보관, 중복 파일 정리
- 이동 경로 흐름도 애니메이션, 정리 후 폴더 구조, 이동 목록(파일 열기·탐색기 보기·중복 원본 보기)
- 모든 단계 진행바, 실행 기록과 되돌리기
- C# WPF + WebView2, Visual Studio 2026 빌드, 단독 실행 exe, 앱 아이콘
- 언어 전환: 한국어 · English · 中文 · 日本語 (화면 문구, 검사 사유, 정리 결과 폴더 이름까지)
- 연결·주의 파일을 프로그램/프로젝트/폴더 단위로 묶어서 표시, 펼쳐서 개별 확인
- 버전과 소유주(Jaelog By 아지랑이) 표시: 창 제목, 화면 왼쪽 아래, exe 파일 속성
