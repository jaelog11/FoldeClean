# 변경 이력

버전은 0.1부터 시작해 기능이 더해질 때마다 0.2, 0.3 … 으로 올립니다.
버전 번호는 `FoldeClean/FoldeClean.csproj` 의 `<Version>` 한 곳에서만 바꾸면 exe 속성·창 제목·화면에 모두 반영됩니다. 파이썬 판은 `app.py` 의 `VERSION` 을 같이 맞춥니다.

## 0.2 (작업 중) Pro 골격과 내 규칙
기능 구분 기준은 `EDITIONS.md` 참고. 무료판의 안전·정리 기능은 그대로 두고, 자동화와 규칙만 Pro 로 나눈다.

- **판 구분**: Standard(무료) / 체험 14일 / Pro. 왼쪽 아래에 현재 판이 표시되고, 누르면 Pro 안내와 체험 시작 버튼이 나온다. 상태는 `%LOCALAPPDATA%\FoldeClean\edition.json`
- **내 규칙 (Pro)**: 정리 방식보다 먼저 적용되는 사용자 규칙
  - 조건: 파일 이름 · 확장자 · 크기 · 지난 날수 · 상위 폴더 이름 · 전체 경로 (여러 조건은 모두 만족해야 함)
  - 비교: 포함 / 포함하지 않음 / 같음 / 로 시작 / 로 끝남 / 정규식 / 보다 큼 / 보다 작음
  - 동작: 이 폴더로 보내기 · 옮기지 않기 · 이름 앞에 붙이기 · 이름 뒤에 붙이기
  - 폴더 값에 치환자 사용: `{year} {month} {day} {ext} {category} {dir}`
  - 편집기에서 "몇 개나 걸리는지 보기" 로 저장 전에 결과를 확인
  - 위에 있는 규칙이 먼저 적용되고, 한 파일에는 규칙 하나만 적용됨
  - 규칙에 걸린 파일은 이동 목록에 규칙 이름이 함께 표시됨
  - 저장 위치 `%LOCALAPPDATA%\FoldeClean\rules.json`
### 중복 파일 미리 알기
전에는 "중복 파일 정리"를 고르고 미리보기를 만들어야 중복이 있는지 알 수 있었다. 없으면 헛걸음이었다.

- 검사할 때 **크기만으로** 중복 가능성을 미리 계산한다. 파일을 읽지 않아 비용이 없다
- 크기가 같은 파일이 하나도 없으면 중복도 있을 수 없으므로 **"중복 없음"을 즉시** 알려 준다
- 정리 방식 카드에 배지 표시: `중복 없음` 또는 `중복 후보 N개`
- 방식을 고르면 설명 옆에 안내가 나온다. 중복이 없으면 다른 방식을 권하고, 있으면
  "최대 얼마를 줄일 수 있는지"와 **정확히 확인** 버튼을 준다
- 정확히 확인은 실제로 내용을 읽어 `N묶음 · 지울 수 있는 파일 N개 · N 확보` 를 보여준다. 진행바가 함께 뜬다
- **읽을 양이 100MB 이하면 방식을 고르는 즉시 자동으로 확정한다.** 버튼을 누를 필요가 없다
- 확정되면 배지와 안내가 확정값으로 바뀐다. `중복 후보 2개` → 확인 후 `중복 없음`
  ("크기만 같았을 뿐입니다" 라고 이유까지 알려 준다)
- 자체 점검에 예상값이 실제값보다 항상 크거나 같은지(상한 보장) 확인 추가

### 옮길 파일이 없을 때
전에는 0건짜리 미리보기 화면으로 넘어가 빈 흐름도만 보였다. 이제 넘어가지 않고 그 자리에서 이유를 알려 준다.

- 중복 정리: 내용이 같은 파일이 없다
- 오래된 파일 보관: 기준보다 오래된 파일이 없다, 기준 일수를 줄이라고 안내
- 이미 제자리: 파일 N개가 이미 이 방식이 정한 자리에 있다
- 내 규칙: 규칙이 파일 N개를 건너뛰도록 되어 있다
- 그 외: 검사 범위를 넓히거나 다른 방식·옵션을 고르라고 안내

### 검사 범위 (중요한 동작 변경)
전에는 고른 폴더의 **하위 폴더 전부**를 훑었다. 바탕화면처럼 안에 폴더를 두고 쓰는 곳에서는
사용자가 예상한 것보다 훨씬 많은 파일이 잡히고(실제 사례: 12만 개), 이미 정리해 둔 폴더의 파일까지
밖으로 끌려 나올 수 있었다. 기본값을 바꾸고 선택할 수 있게 했다.

- 1단계에 **검사 범위** 선택 추가: `이 폴더의 파일만`(기본) / `하위 2단계까지` / `하위 폴더 전부`
- 기본이 "이 폴더의 파일만"이라 눈에 보이는 파일만 정리된다. 하위 폴더는 손대지 않는다
- 검사 결과 위에 안내가 뜬다. 건너뛴 하위 폴더 수를 알려주고 한 번에 다시 검사할 수 있다
- 반대로 하위 폴더까지 검사해 2만 개가 넘으면 경고하고 되돌아갈 버튼을 준다

- 규칙과 판 구분 문구를 4개 언어에 추가
- 자체 점검(`--selftest`)에 규칙 검증 추가: 치환자, 건너뛰기, 이름 앞 붙이기(공백 보존), 조건 두 개

## 0.1.0 (2026-09-13) 첫 공개
GitHub Release 게시: https://github.com/jaelog11/FoldeClean/releases/tag/v0.1.0

**정리 기능**
- 5단계 화면: 폴더 선택 → 검사 → 정리 방식 → 미리보기 → 실행
- 연결 파일 감지: 시스템 폴더, 프로그램 폴더, 레지스트리 등록 경로, 사용 중인 파일(차단) / 바로가기 대상, 실행파일, 프로젝트 폴더, 클라우드 동기화, 숨김(주의)
- 연결·주의 파일을 프로그램/프로젝트/폴더 단위로 묶어서 표시, 펼쳐서 개별 확인
- 정리 방식 7가지: 종류별, 날짜별, 종류→연도, PARA, Johnny.Decimal, 오래된 파일 보관, 중복 파일 정리
- 이동 경로 흐름도 애니메이션, 정리 후 폴더 구조, 이동 목록(파일 열기·탐색기 보기·중복 원본 보기), 개별 제외

**안전**
- 이동 기록을 한 건마다 즉시 저장(.jsonl). 강제 종료돼도 "되돌리기 기록"에서 복구·되돌리기 가능
- 첫 실행 안내 화면 (실행 전 무변경, 자동 제외, 기록·되돌리기, 작은 폴더로 시험)
- 왼쪽 아래 "오류 보고서 저장" 버튼: 바탕화면에 보고서 저장, 오류 로그(%LOCALAPPDATA%\FoldeClean\logs) 포함
- 모든 단계 진행바 (검사, 잠김 확인, 중복 해시, 계획, 실행, 되돌리기)

**기반**
- C# WPF + WebView2, Visual Studio 2026 빌드, 단독 실행 exe, 앱 아이콘
- 언어 전환: 한국어 · English · 中文 · 日本語 (화면 문구, 검사 사유, 정리 결과 폴더 이름까지)
- 버전과 소유주(Jaelog By 아지랑이) 표시: 창 제목, 화면 왼쪽 아래, exe 파일 속성
- 중복 검사 해시 xxHash128 (암호화 해시보다 빠르고 백신 휴리스틱을 덜 자극)
- 성능: 2만 6천 개 기준 탐색 0.05초, 연결 파일 검사 1.5초, 잠김 확인은 병렬 20초 제한

**배포**
- 프리웨어 라이선스(LICENSE.txt), winget 매니페스트(winget/), 배포 묶음 스크립트(release.ps1)
- 폴더 형태 빌드(build.cmd folder), 서명 스크립트(sign.cmd, make_cert.ps1)
